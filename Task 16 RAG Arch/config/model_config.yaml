models:
  llm:
    provider: openai
    model: gpt-5.6-luna  # example, to be configured via environment or actual value
  embedding:
    provider: openai
    model: text-embedding-3-small  # example, to be configured

retrieval:
  candidate_k: 10
  final_top_k: 3

generation:
  temperature: 0
  max_tokens: 500